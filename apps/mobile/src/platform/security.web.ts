/**
 * Web varijanta security (freeRASP) konfiguracije: RASP je native-only i
 * čita android/ios polja expo configa kojih na webu nema — preview ne radi
 * runtime application self-protection. Oblik exporta prati security.ts.
 */
const config = {}
const actions = {}

export { config, actions }
